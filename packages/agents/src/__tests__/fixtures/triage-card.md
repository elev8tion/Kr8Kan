This card is actively being worked on, so it belongs in **Doing**
(`lst1abcd2efg`). The **bug** label applies. Priority read: High — it has a
due date this week.

```json
{
  "listPublicId": "lst1abcd2efg",
  "labelPublicIds": ["lblaaaa1111b"],
  "reasoning": "Work has started per the comments and it is due this week."
}
```
